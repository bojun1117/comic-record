# 複製為 .env(已經 gitignore)後填值。
# 建議從 SSM 撈,跟舊 Lambda 共用同一份 secret,登入過的 token 會繼續有效:
#
#   export JWT_SECRET=$(aws ssm get-parameter --name /comic-vibe/dev/jwt-secret --with-decryption --query Parameter.Value --output text)
#   export APP_PASSWORD=$(aws ssm get-parameter --name /comic-vibe/dev/app-password --with-decryption --query Parameter.Value --output text)
#   echo "JWT_SECRET=$JWT_SECRET" >> .env
#   echo "APP_PASSWORD=$APP_PASSWORD" >> .env

JWT_SECRET=
APP_PASSWORD=
